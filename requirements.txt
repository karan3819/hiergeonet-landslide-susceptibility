# Core numerical / data stack
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10
h5py>=3.8

# Deep learning
# Install torch matching your CUDA version FIRST — see https://pytorch.org/get-started/locally/
# torch_geometric's own install also depends on your torch + CUDA combo, see:
# https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
torch>=2.1
torch_geometric>=2.4

# Classical ML baselines
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# Plotting (imported by train.py and legacy/run_hiergeonet.py; only the
# legacy script actually calls plt.savefig() — see README section 2.3)
matplotlib>=3.7
seaborn>=0.12

# Utilities
tqdm>=4.65
